type Post {
	id: ID!
	text: String!
	poster: User!
	comments: [Comment!]!
	commentCount: Int!
	postVisibility: PostVisibility!
	postedOn: DateTime!
}

enum PostVisibility {
	PUBLIC
	DISTRICT
	FRIENDS
}
