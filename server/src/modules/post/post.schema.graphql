type Post {
	id: ID!
	text: String!
	poster: Profile!
	comments: [Comment!]!
	commentCount: Int!
	postVisibility: PostVisibility!
	postedOn: DateTime!
}

enum PostVisibility {
	PUBLIC
	DISTRICT
	FRIENDS
}
