type Post {
	id: ID!
	text: String!
	poster: User!
	comments: [Comment!]
	usersCommented: Int!
	postVisibility: PostVisibility!
	postedOn: DateTime!
}

enum PostVisibility {
	PUBLIC
	DISTRICT
	PRIVATE
}
