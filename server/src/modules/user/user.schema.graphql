type User {
	email: ID!
	name: String!
	photo: String
	bio: String
	schoolName: String
	schoolAbbreviation: String
	joinedOn: DateTime!
	posts: [Post!]
	friendships: [Friendship!]
	postCount: Int
	commentCount: Int
}
